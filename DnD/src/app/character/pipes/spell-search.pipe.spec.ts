import { SpellSearchPipe } from './spell-search.pipe';

describe('SpellSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SpellSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
